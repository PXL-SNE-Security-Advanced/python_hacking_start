#Een tuple definieren
ip_port = ("192.168.1.1", 80)
version_info = (2, 7, 18)
mijn_tuple = (1, 2, 3, 4, 5)

# Toegang tot elementen in de tuple
print(mijn_tuple[0]) # Geeft: 1
print(mijn_tuple[3]) # Geeft: 4
# De volgorde blijft altijd hetzelfde
print(mijn_tuple) # Geeft: (1, 2, 3, 4, 5


#Situatie geen veranderingen nodig
coordinates = (10.0, 20.0)
print(coordinates[0])  # 10.0



my_tuple = (1, 2, 3)
# my_tuple[1] = 10  # Dit geeft een fout, omdat tuples niet wijzigbaar zijn
